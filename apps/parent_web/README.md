# STUDYON Parent Web

학부모용 읽기 전용 리포트 웹이다. 관리자 웹에서 발급한 parent access token 링크로 진입한다.

## Local run

```bash
npm install
npm run dev
```

기본 포트는 `11113`이다.

```bash
API_URL=http://127.0.0.1:3000 npm run dev
```
