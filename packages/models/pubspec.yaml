name: studyon_models
description: Domain models with freezed for STUDYON
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  freezed_annotation: ^2.4.0
  json_annotation: ^4.9.0
  studyon_core:
    path: ../core

dev_dependencies:
  build_runner: ^2.4.0
  freezed: ^2.5.0
  json_serializable: ^6.9.0
